services:
  vape-server:
    build: ./server
    ports:
      - "5000:5000"
    environment:
      - VAPE_API_TOKEN=${VAPE_API_TOKEN}
      - VAPE_SERVER_URL=${VAPE_SERVER_URL}
    volumes:
      - ./output:/output
    restart: unless-stopped
